# alignment-game

Fresh React + Vite scaffold, ready for a new design.

```bash
npm install
npm run dev
```

The earlier game prototype and design pitch are preserved in git history
(see commits before this one on `main` and on `claude/resume-game-dev-jyuKg`).
